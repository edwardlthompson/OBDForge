# Copy to ~/.obdforge/signing.env and fill in (never commit).
export OBDFORGE_KEYSTORE_PATH="$HOME/.obdforge/obdforge-release.keystore"
export OBDFORGE_KEY_ALIAS="obdforge"
export OBDFORGE_KEYSTORE_PASSWORD="change-me"
export OBDFORGE_KEY_PASSWORD="change-me"
